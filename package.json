{
	"name": "tor-socks-proxy",
	"private": true,
	"version": "0.0.0",
	"description": "Tor SOCKS proxy (default) + Node HTTP proxy with per-country Tor instances",
	"type": "commonjs",
	"main": "src/index.js",
	"scripts": {
		"start": "node src/index.js"
	},
	"dependencies": {
		"socks": "^2.8.7",
		"socks-proxy-agent": "^8.0.5"
	}
}
